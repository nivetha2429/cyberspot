import mongoose from "mongoose";
import dotenv from "dotenv";

dotenv.config();

const seedAdmin = async () => {
    // This script is intentionally left empty to prevent default admin creation.
    console.log("Seeding script disabled.");
    process.exit(0);
};

seedAdmin();
